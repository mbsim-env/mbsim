EHD-Module
===========

Necessary:
===========
	- kernel

Installation:
===========
CMake is used as build system.

1. create a build-directory
   mkdir BUILD
   cd BUILD
   
2. run cmake in the BUILD Directory
   cmake ..
   
   important options:
     -DCMAKE_INSTALL_PREFIX=PREFIX (where to install the library)
    
3. Build (and install) the library
   make 
   (make install)
